extends base

block content
    form(method="POST")
        input(type="text" name="word" placeholder="단어", value=content.word, required)
        textarea(name="explanation", cols="30", rows="10", placeholder="설명", required)=content.explanation
        input(type="submit" value="저장")